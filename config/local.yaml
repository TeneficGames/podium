healthcheck:
  workingText: WORKING

redis:
  addrs:
    - localhost:6379
  host: localhost
  port: 6379
  password: ""
  db: 0

faultyRedis:
  host: "localhost"
  port: 12345
  db: 0

api:
  maxReturnedMembers: 2000

worker:
  expirationCheckInterval: 5s
  expirationLimitPerRun: 1000

enrichment:
  providers:
    dummy_tenant_id:
      endpoint: "http://localhost:8080/v1/members/enrich"
      headers: {}
      mode: best_effort
      retry:
        max_attempts: 3
        initial_backoff: 50ms
        max_backoff: 500ms
  request_timeout: 500ms
  cache:
    ttl: 24h
    addr: "localhost:6379"
    password: ""
